import { logo } from '../../assets';

const Navbar = ()=> {
    return (
        <div>
            <div>
                <img src={logo} />
            </div>
        </div>
    )
}

export default Navbar;